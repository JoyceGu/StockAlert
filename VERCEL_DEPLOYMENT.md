# Vercel 部署故障排除指南

## 🔍 问题：图表不显示 / 没有图片

### 可能的原因：

1. **API路由配置问题**
2. **静态文件无法加载**
3. **CORS问题**
4. **环境变量缺失**

---

## ✅ 解决方案

### 1. 确保 vercel.json 配置正确

已创建 `vercel.json` 文件，确保包含：

```json
{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server.js"
    },
    {
      "src": "/(.*)",
      "dest": "/server.js"
    }
  ]
}
```

### 2. 检查浏览器控制台

打开浏览器开发者工具（F12），查看：
- **Console标签**：是否有JavaScript错误
- **Network标签**：API请求是否成功
  - `/api/stock/AAPL` 应该返回200状态码
  - `styles.css` 和 `script.js` 应该成功加载

### 3. 常见问题修复

#### 问题A：API返回404

**症状**：Network标签显示 `/api/stock/AAPL` 返回404

**解决**：
1. 检查 `vercel.json` 是否存在
2. 确保 `server.js` 在根目录
3. 重新部署到Vercel

#### 问题B：CORS错误

**症状**：Console显示CORS错误

**解决**：`server.js` 中已包含 `cors()` 中间件，应该没问题

#### 问题C：静态文件404

**症状**：`styles.css` 或 `script.js` 无法加载

**解决**：
- 确保文件在根目录
- 检查 `express.static()` 配置
- 清除浏览器缓存

---

## 🚀 部署步骤

### 方法1：通过Vercel CLI

```bash
# 安装Vercel CLI
npm install -g vercel

# 登录
vercel login

# 部署
cd StockAlert
vercel

# 生产环境部署
vercel --prod
```

### 方法2：通过GitHub集成

1. 推送代码到GitHub
2. 在Vercel网站：
   - 点击 "New Project"
   - 导入GitHub仓库
   - 自动检测配置
   - 点击 "Deploy"

---

## 🔧 调试步骤

### 步骤1：检查API端点

在浏览器中访问：
```
https://your-app.vercel.app/api/health
```

应该返回：
```json
{
  "status": "OK",
  "timestamp": "...",
  "uptime": ...
}
```

### 步骤2：测试股票API

访问：
```
https://your-app.vercel.app/api/stock/AAPL
```

应该返回股票数据JSON

### 步骤3：检查前端代码

打开浏览器控制台，查看：
```javascript
// 应该能看到这些日志
console.log('StockAlertApp initialized');
```

### 步骤4：检查网络请求

在Network标签中：
- ✅ `/api/stock/VOO` - 应该返回200
- ✅ `/api/stock/QQQ` - 应该返回200
- ✅ `styles.css` - 应该返回200
- ✅ `script.js` - 应该返回200

---

## 🐛 常见错误修复

### 错误1：`Cannot GET /api/stock/AAPL`

**原因**：Vercel路由配置问题

**修复**：
1. 检查 `vercel.json` 文件
2. 确保路由配置正确
3. 重新部署

### 错误2：`Loading stock data...` 一直显示

**原因**：API调用失败或超时

**修复**：
1. 检查浏览器Network标签
2. 查看API请求是否成功
3. 检查Yahoo Finance API是否可访问
4. 查看Vercel函数日志

### 错误3：`Chart is not defined`

**原因**：Chart.js库未加载

**修复**：
1. 检查网络连接
2. 检查CDN链接是否可访问
3. 尝试使用本地Chart.js文件

---

## 📝 Vercel函数日志

查看日志：
1. 登录Vercel
2. 选择你的项目
3. 点击 "Functions" 标签
4. 查看 `server.js` 的日志

常见日志：
- ✅ `🚀 Stock Alert Server running` - 正常
- ❌ `Error fetching data` - API错误
- ❌ `No data found` - 股票代码无效

---

## 🔄 重新部署

如果问题持续：

```bash
# 清除Vercel缓存
vercel --force

# 或者删除并重新部署
vercel remove your-project-name
vercel
```

---

## ✅ 验证清单

部署后检查：

- [ ] `vercel.json` 文件存在
- [ ] `server.js` 在根目录
- [ ] 所有静态文件（HTML, CSS, JS）在根目录
- [ ] `/api/health` 端点工作
- [ ] `/api/stock/AAPL` 返回数据
- [ ] 浏览器控制台无错误
- [ ] Network标签显示所有请求成功

---

## 🆘 如果仍然无法解决

1. **检查Vercel Dashboard**：
   - Functions标签 → 查看错误日志
   - Deployments标签 → 查看部署状态

2. **检查代码**：
   - 确保 `server.js` 导出正确
   - 确保路由配置正确

3. **测试本地**：
   ```bash
   npm start
   # 访问 http://localhost:3000
   # 如果本地工作，问题在Vercel配置
   ```

4. **联系支持**：
   - Vercel社区论坛
   - GitHub Issues

---

## 📚 参考资源

- [Vercel Node.js文档](https://vercel.com/docs/functions/serverless-functions/runtimes/node-js)
- [Vercel路由配置](https://vercel.com/docs/project-configuration)
- [Express on Vercel](https://vercel.com/guides/using-express-with-vercel)

---

**部署后，请检查浏览器控制台和Network标签来诊断问题！** 🔍
